@namespace("cwl.draft-2")
protocol Datatype {

  enum Datatype {
    NULL,
    BOOLEAN,
    INT,
    LONG,
    FLOAT,
    DOUBLE,
    BYTES,
    STRING,
    RECORD,
    ENUM,
    ARRAY,
    MAP,
    FILE,
    ANY
  }
}
